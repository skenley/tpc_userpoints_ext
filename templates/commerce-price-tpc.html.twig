{#
/**
 * @file
 * Default theme implementation for displaying a price.
 *
 * Used by the 'commerce_price_tpc' formatter.
 * Available variables:
 *   - number: The number.
 *   - currency: The currency entity.
 *
 * @ingroup themeable
 */
#}
{{ number|number_format(0, '.', ',') }} points
